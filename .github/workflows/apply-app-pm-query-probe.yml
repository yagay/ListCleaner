name: Apply App PM Query Probe

on:
  push:
    paths:
      - tools/apply_app_pm_query_probe.py
      - .github/workflows/apply-app-pm-query-probe.yml

permissions:
  contents: write

jobs:
  patch-build-commit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
      - name: Apply patch
        run: python3 tools/apply_app_pm_query_probe.py
      - name: Verify probe
        run: |
          grep -q 'APP_PM_QUERY_PROBE_READY' app/src/main/java/com/yagay/ListCleaner/xposed/ListCleanerModule.kt
          grep -Fq 'IPackageManager\$Stub\$Proxy' app/src/main/java/com/yagay/ListCleaner/xposed/ListCleanerModule.kt
          grep -q 'APP_PM_QUERY_PROBE_HOOK_ID' app/src/main/java/com/yagay/ListCleaner/xposed/ListCleanerModule.kt
      - name: Build and test
        run: |
          chmod +x gradlew
          ./gradlew testDebugUnitTest assembleDebug --stacktrace
      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: ListCleaner-app-pm-query-probe-debug
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
      - name: Cleanup one-shot files
        run: |
          rm -f tools/apply_app_pm_query_probe.py
          rm -f .github/workflows/apply-app-pm-query-probe.yml
      - name: Commit implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Add app-side IPackageManager query probe' || exit 0
          git push
