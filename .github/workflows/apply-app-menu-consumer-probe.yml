name: Apply App Menu Consumer Probe

on:
  push:
    paths:
      - tools/apply_app_menu_consumer_probe.py
      - .github/workflows/apply-app-menu-consumer-probe.yml

permissions:
  contents: write

jobs:
  patch-build-commit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
      - name: Apply patch
        run: python3 tools/apply_app_menu_consumer_probe.py
      - name: Verify probe
        run: |
          grep -Fq 'APP_MENU_CONSUMER_READY' app/src/main/java/com/yagay/ListCleaner/xposed/ListCleanerModule.kt
          grep -Fq 'ic-app-menu-consumer-probe' app/src/main/java/com/yagay/ListCleaner/xposed/ListCleanerModule.kt
          grep -Fq 'installAppMenuConsumerProbe' app/src/main/java/com/yagay/ListCleaner/xposed/ListCleanerModule.kt
      - name: Build and test
        run: |
          chmod +x gradlew
          ./gradlew testDebugUnitTest assembleDebug --stacktrace
      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: ListCleaner-app-menu-consumer-probe-debug
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
      - name: Cleanup one-shot files
        run: |
          rm -f tools/apply_app_menu_consumer_probe.py
          rm -f .github/workflows/apply-app-menu-consumer-probe.yml
      - name: Commit implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Add generic app menu consumer probe' || exit 0
          git push
