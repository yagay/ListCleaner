name: Apply Virtual Disable Cleanup

on:
  push:
    branches: [main]
    paths:
      - 'tools/patch-virtual-disable.py'
      - '.github/workflows/apply-virtual-disable-cleanup.yml'

permissions:
  contents: write

env:
  JAVA_VERSION: '17'

jobs:
  patch-build-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Apply source cleanup and virtual-disable patch
        run: python3 tools/patch-virtual-disable.py

      - name: Verify no obsolete adaptive patch code remains
        run: |
          ! grep -R "ADAPTIVE_\|AdaptiveContext\|adaptiveFilterCollection\|adaptivePackageName" app/src/main/java || exit 1
          test ! -e app/adaptive-chooser-build-marker.txt
          test ! -e tools/patch-app-layer.py
          test ! -e .github/workflows/apply-strict-app-layer.yml

      - name: Build and test
        run: ./gradlew :app:testDebugUnitTest :app:assembleDebug --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: ListCleaner-virtual-disable-debug
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      - name: Remove one-shot patch files
        run: |
          rm -f tools/patch-virtual-disable.py
          rm -f .github/workflows/apply-virtual-disable-cleanup.yml

      - name: Commit cleaned implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Add virtual component disable layer and remove adaptive leftovers"
          git push
