name: Apply App Hook Package Fallback

on:
  push:
    branches: [main]
    paths:
      - tools/patch-app-package-fallback.py
      - .github/workflows/apply-app-package-fallback.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Apply patch
        run: python tools/patch-app-package-fallback.py
      - name: Remove temporary patch files
        run: rm tools/patch-app-package-fallback.py .github/workflows/apply-app-package-fallback.yml
      - name: Commit patch
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Add package fallback for app chooser Hook"
          git push
